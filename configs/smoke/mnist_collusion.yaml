nodes: 3
rounds: 2
epochs: 1
seed: 666
protocol: memory
framework: pytorch
aggregator: fedavg
topology: full
batch_size: 128
show_metrics: false
measure_time: false
save_csv: true
output_dir: results/collusion-validation/attacked
dataset: {name: p2pfl/MNIST, distribution: iid, reduced: true, partition_multiplier: 50}
attack:
  name: collusion
  adversaries: [1, 2]
  parameters:
    strategy: coordinated_aligned_update
    group_id: mnist-smoke-colluders
    group_members: [1, 2]
    seed: 202503
    alpha: 1.0
